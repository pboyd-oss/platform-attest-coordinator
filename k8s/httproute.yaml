apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: platform-attest-coordinator
  namespace: platform
  labels:
    external-dns: "true"
  annotations:
    external-dns.alpha.kubernetes.io/target: tuxgrid.com
spec:
  parentRefs:
  - kind: Gateway
    name: gateway-external
    namespace: gateway
    sectionName: https
  hostnames:
  - attest.platform.tuxgrid.com
  rules:
  - backendRefs:
    - name: platform-attest-coordinator
      port: 80
