apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: platform-attest-coordinator
  namespace: platform
spec:
  parentRefs:
  - kind: Gateway
    name: gateway-internal
    namespace: gateway
  hostnames:
  - attest.tuxgrid.com
  rules:
  - backendRefs:
    - name: platform-attest-coordinator
      port: 80
